<section>
    <h2>{{post.title}}</h2>
    <p>{{post.post_text}}</p>
    <h3>Comments</h3>
    {{#each post.comments as |comment|}}
    <div><p>{{comment.comment_text}}</p></div>
    {{/each}}

</section>